from setuptools import find_packages, setup

setup(
    name='pkg_helpers',
    packages=find_packages(
        where="."
    ),
    version='latest',
    description='pkg_helpers',
    author='pkg_helpers',
)